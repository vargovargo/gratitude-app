{
  "name": "gratitude-text-app",
  "version": "1.0.0",
  "description": "Daily gratitude SMS app — sends rotating morning prompts to family members, saves responses, tracks streaks, and sends evening reminders.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "better-sqlite3": "^9.4.3",
    "dotenv": "^16.4.1",
    "express": "^4.18.2",
    "node-cron": "^3.0.3",
    "twilio": "^5.3.3"
  },
  "devDependencies": {
    "nodemon": "^3.0.3"
  }
}
